# Build output — regenerated by `python build.py`; the deploy Action rebuilds it
# fresh on every push, so it never needs to live in the repo.
_site/

# Python bytecode
__pycache__/
*.pyc

# macOS cruft
.DS_Store

# Scratch / temp working files
scratchpad/
