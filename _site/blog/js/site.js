var ToggleClose=(function(){var toggle=document.getElementById('theme-toggle');var home=null,placeholder=null,activeClose=null;function lift(){if(!toggle||home)return;home=toggle.parentNode;var r=toggle.getBoundingClientRect();placeholder=document.createElement('div');placeholder.setAttribute('aria-hidden','true');placeholder.style.width=r.width+'px';placeholder.style.height=r.height+'px';home.insertBefore(placeholder,toggle);toggle.style.position='fixed';toggle.style.top=r.top+'px';toggle.style.left=r.left+'px';toggle.style.width=r.width+'px';toggle.style.height=r.height+'px';toggle.style.margin='0';toggle.style.zIndex='1001';document.body.appendChild(toggle);['.ts-pill','.ts-cross','.toggle-knob'].forEach(function(sel){var el=toggle.querySelector(sel);if(el)el.style.animation='none';});}
function rehome(){if(!toggle||!home)return;toggle.classList.remove('as-close');toggle.removeAttribute('style');if(placeholder&&placeholder.parentNode){placeholder.parentNode.insertBefore(toggle,placeholder);placeholder.parentNode.removeChild(placeholder);}else{home.appendChild(toggle);}
placeholder=null;home=null;}
if(toggle)toggle.addEventListener('click',function(){if(activeClose)activeClose();});return{engage:function(closeFn){activeClose=closeFn;lift();requestAnimationFrame(function(){if(toggle)toggle.classList.add('as-close');});},startClose:function(){if(toggle)toggle.classList.remove('as-close');},finishClose:function(){activeClose=null;rehome();try{window.dispatchEvent(new Event('scroll'));}catch(e){}},isEngaged:function(){return!!activeClose;}};})();var Toast=(function(){var el=null,t=null;return{show:function(text,anchor){if(!el){el=document.createElement('div');el.className='toast-pill';document.body.appendChild(el);}
el.textContent=text;el.classList.add('show');var r=anchor.getBoundingClientRect();el.style.top=(r.bottom+10)+'px';el.style.left=Math.max(8,Math.min(r.left+r.width/2-el.offsetWidth/2,window.innerWidth-8-el.offsetWidth))+'px';clearTimeout(t);t=setTimeout(function(){el.classList.remove('show');},2000);}};})();var Outline={make:function(cls){var NS='http://www.w3.org/2000/svg';var svg=document.createElementNS(NS,'svg');svg.setAttribute('class','draw-outline '+cls);svg.setAttribute('aria-hidden','true');svg.appendChild(document.createElementNS(NS,'rect'));return svg;},size:function(svg,el,rx){var w=el.offsetWidth,h=el.offsetHeight;if(!w||!h)return 0;svg.setAttribute('viewBox','0 0 '+w+' '+h);var rect=svg.firstChild;rect.setAttribute('x',0.5);rect.setAttribute('y',0.5);rect.setAttribute('width',w-1);rect.setAttribute('height',h-1);rect.setAttribute('rx',rx);rect.setAttribute('ry',rx);var len=rect.getTotalLength?rect.getTotalLength():2*(w+h);rect.style.setProperty('--len',len);return len;}};(function(){const overlay=document.getElementById('search-overlay');const input=document.getElementById('search-input');const inputWrap=document.getElementById('search-input-wrap');const results=document.getElementById('search-results');const clearBtn=document.getElementById('search-clear');const toggle=document.getElementById('theme-toggle');let toggleHome=null;let togglePlaceholder=null;let INDEX=null;let isOpen=false;let selectedIndex=-1;let closeTimeout=null;const EXIT_DURATION=800;function navigableLis(){return Array.from(results.querySelectorAll('li')).filter(li=>li.querySelector('a')&&!li.classList.contains('search-empty'));}
function updateSelection(){const lis=navigableLis();if(selectedIndex>=lis.length)selectedIndex=lis.length-1;lis.forEach((li,i)=>li.classList.toggle('selected',i===selectedIndex));if(selectedIndex>=0&&lis[selectedIndex])lis[selectedIndex].scrollIntoView({block:'nearest'});}
function formatDate(date){const[year,month]=date.split('-');return`${year}<span class="separator">&middot;</span>${month}`;}
function escapeHtml(s){return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}
function render(query){if(!INDEX){results.innerHTML='';return;}
const q=(query||'').toLowerCase().trim();let items=q?INDEX.filter(p=>(p.tags||[]).some(t=>t.toLowerCase().includes(q))||p.title.toLowerCase().includes(q)||p.text.toLowerCase().includes(q)):INDEX;const posts=items.filter(p=>p.type==='post').slice().sort((a,b)=>b.date.localeCompare(a.date));const pages=items.filter(p=>p.type==='page');items=posts.concat(pages);if(!items.length){results.innerHTML='<li class="search-empty">No result found. Do <a href="mailto:ward.nicolas@outlook.com">reach out</a> if you want to chat about a topic not mentioned here!</li>';return;}
const n=items.length;overlay.style.setProperty('--n',n);results.innerHTML=items.map((p,i)=>{const prefix=p.type==='post'?`<span class="post-date">${formatDate(p.date)}</span>`:`<span class="post-date">page</span>`;return`<li style="--i:${i}"><a href="/blog/${p.slug}">${prefix}<span class="search-title">${escapeHtml(p.title)}</span></a></li>`;}).join('');selectedIndex=-1;updateSelection();}
function ensureIndex(cb){if(INDEX){cb();return;}
fetch('/blog/search-index.json').then(r=>r.json()).then(data=>{INDEX=data;cb();});}
function homeToggle(){ToggleClose.finishClose();}
function open(initialQuery){if(closeTimeout){clearTimeout(closeTimeout);closeTimeout=null;overlay.classList.remove('closing');}
var nb=document.getElementById('nav-search');if(nb){var nr=nb.getBoundingClientRect();var cx=nr.left+nr.width/2,cy=nr.top+nr.height/2;var rad=Math.hypot(Math.max(cx,innerWidth-cx),Math.max(cy,innerHeight-cy));overlay.style.setProperty('--sx',cx+'px');overlay.style.setProperty('--sy',cy+'px');overlay.style.setProperty('--sr',rad+'px');}
ToggleClose.engage(close);document.documentElement.classList.add('searching');overlay.classList.add('open');overlay.setAttribute('aria-hidden','false');document.documentElement.style.overflow='hidden';isOpen=true;if(initialQuery!==undefined)input.value=initialQuery;syncClear();var isTouch=navigator.maxTouchPoints>0||(window.matchMedia&&matchMedia('(pointer: coarse)').matches);if(isTouch){setTimeout(function(){input.focus();var len=input.value.length;input.setSelectionRange(len,len);},540);}else{input.focus();input.setSelectionRange(input.value.length,input.value.length);requestAnimationFrame(()=>{input.focus();const len=input.value.length;input.setSelectionRange(len,len);});}
ensureIndex(()=>render(input.value));}
function close(){if(!isOpen&&!overlay.classList.contains('closing'))return;overlay.classList.remove('open');overlay.classList.add('closing');overlay.setAttribute('aria-hidden','true');isOpen=false;input.blur();ToggleClose.startClose();if(closeTimeout)clearTimeout(closeTimeout);closeTimeout=setTimeout(()=>{overlay.classList.remove('closing');document.documentElement.style.overflow='';document.documentElement.classList.remove('searching');ToggleClose.finishClose();closeTimeout=null;},EXIT_DURATION);}
function syncClear(){inputWrap.classList.toggle('has-value',input.value.length>0);}
input.addEventListener('input',e=>{render(e.target.value);syncClear();});inputWrap.addEventListener('animationend',function(e){if(e.animationName!=='search-fade-in')return;const len=input.value.length;input.focus();input.setSelectionRange(len,len);});clearBtn.addEventListener('click',()=>{input.value='';syncClear();render('');input.focus();});input.addEventListener('keydown',function(e){const lis=navigableLis();if(e.key==='ArrowDown'){if(!lis.length)return;e.preventDefault();selectedIndex=selectedIndex===-1?0:(selectedIndex+1)%lis.length;updateSelection();}else if(e.key==='ArrowUp'){if(!lis.length)return;e.preventDefault();selectedIndex=selectedIndex===-1?lis.length-1:(selectedIndex-1+lis.length)%lis.length;updateSelection();}else if(e.key==='Enter'){if(!lis.length)return;e.preventDefault();const idx=selectedIndex===-1?0:selectedIndex;const link=lis[idx]&&lis[idx].querySelector('a');if(link)window.location.href=link.getAttribute('href');}});overlay.addEventListener('mousedown',e=>{if(e.target===overlay)close();});document.addEventListener('click',function(e){const trigger=e.target.closest('[data-search]');if(!trigger)return;e.preventDefault();open(trigger.getAttribute('data-search'));});const navSearch=document.getElementById('nav-search');if(navSearch)navSearch.addEventListener('click',()=>open());const searchClose=document.getElementById('search-close');if(searchClose)searchClose.addEventListener('click',()=>close());document.addEventListener('keydown',function(e){const tag=(document.activeElement&&document.activeElement.tagName)||'';const inField=(tag==='INPUT'||tag==='TEXTAREA'||tag==='SELECT');if((e.key==='s'||e.key==='S')&&!inField&&!isOpen&&!e.metaKey&&!e.ctrlKey&&!e.altKey){e.preventDefault();e.stopPropagation();open();}else if(e.key==='Escape'&&isOpen){e.preventDefault();e.stopPropagation();close();}},true);})();(function(){const widget=document.getElementById('reading-progress');if(!widget)return;const article=document.querySelector('article .post-content');if(!article)return;const fill=widget.querySelector('.progress-fill');const percentEl=widget.querySelector('.progress-percent');const CIRCUMFERENCE=2*Math.PI*19;const reduceMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;let wasComplete=false,firstRun=true;let entered=false,countingUp=false,currentPercent=0;function burst(){if(reduceMotion)return;const rect=widget.getBoundingClientRect();const cx=rect.left+rect.width/2;const cy=rect.top+rect.height/2;for(let i=0;i<29;i++){const piece=document.createElement('div');piece.className='confetti-piece';const angle=Math.random()*Math.PI*2;const dist=55+Math.random()*85;piece.style.left=cx+'px';piece.style.top=cy+'px';piece.style.setProperty('--tx',(Math.cos(angle)*dist).toFixed(1)+'px');piece.style.setProperty('--ty',(Math.sin(angle)*dist).toFixed(1)+'px');piece.style.setProperty('--rot',Math.round(Math.random()*720-360)+'deg');piece.style.setProperty('--size',(6+Math.random()*5).toFixed(1)+'px');piece.style.setProperty('--b',(0.85+Math.random()*0.4).toFixed(2));piece.style.setProperty('--dur',Math.round(650+Math.random()*450)+'ms');piece.addEventListener('animationend',function(){piece.remove();});document.body.appendChild(piece);}}
function update(){const rect=article.getBoundingClientRect();const articleHeight=article.offsetHeight;const viewportHeight=window.innerHeight;const READING_LINE=0.5;const seen=viewportHeight*READING_LINE-rect.top;const progress=Math.max(0,Math.min(1,seen/articleHeight));const percent=Math.round(progress*100);currentPercent=percent;if(entered&&!countingUp)percentEl.textContent=percent+'%';if(entered)fill.style.strokeDashoffset=CIRCUMFERENCE*(1-progress);const done=percent>=100;widget.classList.toggle('complete',done);widget.title=done?'Back to top':'Skip to end';if(done&&!wasComplete&&!firstRun)burst();wasComplete=done;firstRun=false;}
let ticking=false;function onScroll(){if(ticking)return;ticking=true;requestAnimationFrame(()=>{update();ticking=false;});}
function countUp(target,duration){countingUp=true;if(target<=0){percentEl.textContent='0%';countingUp=false;return;}
const start=performance.now();(function frame(now){const t=Math.min(1,(now-start)/duration);const eased=1-Math.pow(1-t,3);percentEl.textContent=Math.round(eased*target)+'%';if(t<1){requestAnimationFrame(frame);}else{countingUp=false;percentEl.textContent=currentPercent+'%';}})(start);}
update();if(reduceMotion){entered=true;update();}else{setTimeout(function(){fill.style.transition='stroke-dashoffset 0.7s cubic-bezier(0.22, 1, 0.36, 1)';entered=true;countingUp=true;update();countUp(currentPercent,700);setTimeout(function(){fill.style.transition='stroke-dashoffset 0.15s ease, stroke 0.35s ease';},750);},1700);}
window.addEventListener('scroll',onScroll,{passive:true});window.addEventListener('resize',onScroll,{passive:true});widget.addEventListener('click',function(){const toBottom=!widget.classList.contains('complete');window.scrollTo({top:toBottom?document.documentElement.scrollHeight:0,behavior:'smooth'});});})();(function(){var topRow=document.querySelector('.post-header .post-share');var section=document.querySelector('.post-share-section');if(!topRow||!section)return;function check(){section.classList.remove('is-redundant');var top=topRow.getBoundingClientRect().top+window.scrollY;var bot=section.getBoundingClientRect().bottom+window.scrollY;if(bot-top<window.innerHeight)section.classList.add('is-redundant');}
check();window.addEventListener('load',check);window.addEventListener('resize',check,{passive:true});})();(function(){const toggle=document.getElementById('theme-toggle');const html=document.documentElement;const themeColorMeta=document.getElementById('theme-color-meta');const THEME_COLORS={light:'#f6f8fa',dark:'#100F0F'};function applyTheme(theme){html.setAttribute('data-theme',theme);if(themeColorMeta)themeColorMeta.setAttribute('content',THEME_COLORS[theme]||THEME_COLORS.light);}
function setTheme(theme){applyTheme(theme);localStorage.setItem('theme-choice',theme);}
const systemDark=window.matchMedia('(prefers-color-scheme: dark)');localStorage.removeItem('theme');const choice=localStorage.getItem('theme-choice');applyTheme(choice?choice:(systemDark.matches?'dark':'light'));function followSystem(){if(!localStorage.getItem('theme-choice'))applyTheme(systemDark.matches?'dark':'light');}
if(systemDark.addEventListener)systemDark.addEventListener('change',followSystem);else if(systemDark.addListener)systemDark.addListener(followSystem);document.addEventListener('visibilitychange',function(){if(!document.hidden)followSystem();});window.addEventListener('pageshow',followSystem);function toggleTheme(){if(ToggleClose.isEngaged())return;const next=html.getAttribute('data-theme')==='dark'?'light':'dark';const reduce=window.matchMedia&&matchMedia('(prefers-reduced-motion: reduce)').matches;if(navigator.vibrate){try{navigator.vibrate(8);}catch(e){}}
if(!document.startViewTransition||reduce){setTheme(next);return;}
html.classList.add('vt-toggling');const vt=document.startViewTransition(()=>setTheme(next));vt.finished.finally(function(){html.classList.remove('vt-toggling');});}
toggle.addEventListener('click',toggleTheme);document.addEventListener('keydown',function(e){const tag=(document.activeElement&&document.activeElement.tagName)||'';if(tag==='INPUT'||tag==='TEXTAREA'||tag==='SELECT')return;if(e.metaKey||e.ctrlKey||e.altKey)return;if(e.key==='t'||e.key==='T'){e.preventDefault();e.stopPropagation();toggleTheme();}else if(e.key==='h'||e.key==='H'){e.preventDefault();e.stopPropagation();window.location.href='/blog';}else if(e.key==='ArrowLeft'||e.key==='ArrowRight'){if(ToggleClose.isEngaged())return;var sib=document.querySelector('.post-siblings');if(!sib)return;var url=sib.getAttribute(e.key==='ArrowLeft'?'data-newer-url':'data-older-url');if(url){e.preventDefault();e.stopPropagation();window.location.href=url;}}},true);var footerKeys=document.querySelector('.footer-keys');if(footerKeys)footerKeys.addEventListener('click',function(e){var btn=e.target.closest('.key-hint[data-act]');if(!btn)return;var act=btn.getAttribute('data-act');if(act==='search'){var s=document.getElementById('nav-search');if(s)s.click();}
else if(act==='theme'){if(toggle)toggle.click();}
else if(act==='home'){window.location.href='/blog';}});})();(function(){if(!document.body.classList.contains('home'))return;var heads=document.querySelectorAll('.site-nav > a');for(var h=0;h<heads.length;h++){heads[h].style.setProperty('--hi',h);heads[h].classList.add('head-in');}
var rows=document.querySelectorAll('main .section-label, main .latest-card, main .section-divider, '+'main .post-list li, main .topics-inline');for(var i=0;i<rows.length;i++){rows[i].style.setProperty('--i',i);rows[i].classList.add('roll-in');}})();(function(){if(!document.body.classList.contains('article')&&!document.body.classList.contains('page'))return;var p=document.querySelector('.post-content > p');if(!p)return;var walker=document.createTreeWalker(p,NodeFilter.SHOW_TEXT,null);var node=walker.nextNode();while(node&&!node.nodeValue.trim())node=walker.nextNode();if(!node)return;var text=node.nodeValue;var i=0;while(i<text.length&&/\s/.test(text[i]))i++;if(i>=text.length||!/[A-Za-z0-9]/.test(text[i]))return;var SVGNS='http://www.w3.org/2000/svg';var span=document.createElement('span');span.className='dropcap';var fill=document.createElement('span');fill.className='dropcap-fill';var svg=document.createElementNS(SVGNS,'svg');svg.setAttribute('class','dropcap-outline');svg.setAttribute('viewBox','0 0 100 100');svg.setAttribute('preserveAspectRatio','none');svg.setAttribute('aria-hidden','true');var rect=document.createElementNS(SVGNS,'rect');rect.setAttribute('x','3');rect.setAttribute('y','3');rect.setAttribute('width','94');rect.setAttribute('height','94');rect.setAttribute('rx','8');rect.setAttribute('ry','8');svg.appendChild(rect);var letter=document.createElement('span');letter.className='dropcap-letter';letter.textContent=text[i];span.appendChild(fill);span.appendChild(svg);span.appendChild(letter);var parent=node.parentNode;if(i>0)parent.insertBefore(document.createTextNode(text.slice(0,i)),node);parent.insertBefore(span,node);parent.insertBefore(document.createTextNode(text.slice(i+1)),node);parent.removeChild(node);if(window.matchMedia&&matchMedia('(prefers-reduced-motion: reduce)').matches)return;try{rect.style.setProperty('--cap-len',rect.getTotalLength());}catch(e){}
requestAnimationFrame(function(){span.classList.add('draw');});})();(function(){if(!document.body.classList.contains('article')&&!document.body.classList.contains('page'))return;var content=document.querySelector('.post-content');if(!content)return;if(window.matchMedia&&matchMedia('(prefers-reduced-motion: reduce)').matches){content.style.opacity=1;return;}
var items=Array.prototype.slice.call(content.children);['.post-share-section','.linked-mentions','.related-posts'].forEach(function(sel){var s=document.querySelector('article '+sel);if(s)items.push(s);});var foot=document.querySelector('.site-footer');if(foot)items.push(foot);var threshold=window.innerHeight*0.92;var baseDelay=document.querySelector('.post-header .post-share')?0.78:0.4;var below=[],open=[];items.forEach(function(el){if(el.getBoundingClientRect().top<threshold)open.push(el);else below.push(el);});below.forEach(function(el){el.classList.add('reveal-up');});open.forEach(function(el,i){if(!el.animate)return;var anim=el.animate([{opacity:0,transform:'translateY(24px)'},{opacity:1,transform:'none'}],{duration:900,delay:(baseDelay+i*0.1)*1000,easing:'cubic-bezier(0.22, 1, 0.36, 1)',fill:'both'});anim.finished.then(function(){anim.cancel();}).catch(function(){});});content.style.opacity=1;if(below.length&&'IntersectionObserver'in window){var obs=new IntersectionObserver(function(entries){entries.forEach(function(e){if(e.isIntersecting){e.target.classList.add('in');obs.unobserve(e.target);}});},{rootMargin:'0px 0px -12% 0px'});below.forEach(function(el){obs.observe(el);});}else{below.forEach(function(el){el.classList.add('in');});}})();(function(){if(window.matchMedia&&matchMedia('(prefers-reduced-motion: reduce)').matches)return;var cards=[];Array.prototype.forEach.call(document.querySelectorAll('.newsletter'),function(nl){if(nl.closest('.subscribe-overlay'))return;var card=nl.querySelector('.newsletter-card');if(!card)return;card.classList.add('has-draw');var cardSvg=Outline.make('draw-card');card.appendChild(cardSvg);cards.push({card:card,cardSvg:cardSvg,co:cardSvg.firstChild,cl:0});});if(!cards.length)return;function measure(){cards.forEach(function(c){c.cl=Outline.size(c.cardSvg,c.card,20);});}
function clamp(v){return v<0?0:v>1?1:v;}
function draw(){var vh=window.innerHeight;var sy=window.scrollY||window.pageYOffset||0;var maxScroll=document.documentElement.scrollHeight-vh;var nearBottom=maxScroll>0?clamp((sy-(maxScroll-vh*0.5))/(vh*0.5)):1;cards.forEach(function(c){var top=c.card.getBoundingClientRect().top;var p=Math.max(clamp((vh*0.7-top)/(vh*0.4)),nearBottom);var cp=clamp(p/0.55);var flush=clamp((p-0.55)/0.45);c.co.style.strokeDashoffset=c.cl*(1-cp);c.card.style.setProperty('--flush',flush);});}
var ticking=false;function onScroll(){if(ticking)return;ticking=true;requestAnimationFrame(function(){draw();ticking=false;});}
measure();draw();window.addEventListener('scroll',onScroll,{passive:true});window.addEventListener('resize',function(){measure();draw();});window.addEventListener('load',function(){measure();draw();});})();(function(){document.querySelectorAll('.share-copy').forEach(function(btn){btn.addEventListener('click',function(){var flash=function(){btn.classList.add('copied');setTimeout(function(){btn.classList.remove('copied');},1500);Toast.show('Copied to clipboard',btn);};if(navigator.clipboard&&navigator.clipboard.writeText){navigator.clipboard.writeText(location.href).then(flash,flash);}else{var ta=document.createElement('textarea');ta.value=location.href;ta.style.cssText='position:fixed;opacity:0';document.body.appendChild(ta);ta.select();try{document.execCommand('copy');}catch(e){}
document.body.removeChild(ta);flash();}});});})();(function(){if(!document.body.classList.contains('article'))return;if(!('ontouchstart'in window)&&!navigator.maxTouchPoints)return;var data=document.querySelector('.post-siblings');var main=document.querySelector('main');if(!data||!main)return;var newer={url:data.getAttribute('data-newer-url'),title:data.getAttribute('data-newer-title')};var older={url:data.getAttribute('data-older-url'),title:data.getAttribute('data-older-title')};var edgeL=document.querySelector('.swipe-edge--left');var edgeR=document.querySelector('.swipe-edge--right');if(newer.url&&edgeL)edgeL.querySelector('.swipe-title').textContent=newer.title;if(older.url&&edgeR)edgeR.querySelector('.swipe-title').textContent=older.title;var W=function(){return window.innerWidth;};var startX=0,startY=0,dx=0,lock=null,active=false;function setX(x){main.style.transform=x?'translateX('+x+'px)':'';}
function showHint(edge,on,mag){if(!edge)return;edge.style.opacity=on?Math.min(1,mag).toFixed(3):0;edge.style.transform='translateY(-50%) translateX('+(on?(edge===edgeL?1:-1)*Math.min(1,mag)*10:0)+'px)';}
function onStart(e){if(e.touches.length>1)return;startX=e.touches[0].clientX;startY=e.touches[0].clientY;dx=0;lock=null;active=true;main.style.transition='none';}
function onMove(e){if(!active||e.touches.length>1)return;var ddx=e.touches[0].clientX-startX,ddy=e.touches[0].clientY-startY;if(lock===null){if(Math.abs(ddx)<8&&Math.abs(ddy)<8)return;lock=Math.abs(ddx)>Math.abs(ddy)?'h':'v';if(lock==='h')document.body.classList.add('swiping');}
if(lock!=='h')return;e.preventDefault();dx=ddx;var hasTarget=dx>0?!!newer.url:!!older.url;var eff=hasTarget?dx:dx*0.22;setX(eff);var mag=Math.abs(eff)/(W()*0.3);showHint(edgeL,dx>0&&!!newer.url,mag);showHint(edgeR,dx<0&&!!older.url,mag);}
function onEnd(){if(!active)return;active=false;if(lock!=='h'){document.body.classList.remove('swiping');return;}
var hasTarget=dx>0?!!newer.url:!!older.url;var target=dx>0?newer:older;main.style.transition='transform 0.34s cubic-bezier(0.22, 1, 0.36, 1)';if(hasTarget&&Math.abs(dx)>W()*0.28){if(navigator.vibrate){try{navigator.vibrate(10);}catch(e){}}
requestAnimationFrame(function(){setX((dx>0?1:-1)*W());});setTimeout(function(){location.href=target.url;},300);}else{showHint(edgeL,false,0);showHint(edgeR,false,0);requestAnimationFrame(function(){setX(0);});setTimeout(function(){document.body.classList.remove('swiping');main.style.transition='';},340);}}
main.addEventListener('touchstart',onStart,{passive:true});main.addEventListener('touchmove',onMove,{passive:false});main.addEventListener('touchend',onEnd);main.addEventListener('touchcancel',onEnd);})();(function(){var header=document.querySelector('.site-header');if(!header)return;var rect=header.querySelector('.pill-outline rect');var svg=header.querySelector('.pill-outline');function measure(){if(!rect)return;if(svg)svg.style.height=header.offsetHeight+'px';var rad=header.offsetHeight/2;rect.setAttribute('rx',rad);rect.setAttribute('ry',rad);if(rect.getTotalLength){header.classList.remove('pill-armed');try{rect.style.setProperty('--pill-len',rect.getTotalLength());}catch(e){}
requestAnimationFrame(function(){header.classList.add('pill-armed');});}}
measure();if(document.fonts&&document.fonts.ready)document.fonts.ready.then(measure);var ticking=false;function update(){ticking=false;if(ToggleClose.isEngaged())return;header.classList.toggle('scrolled',window.scrollY>4);}
window.addEventListener('scroll',function(){if(!ticking){ticking=true;requestAnimationFrame(update);}},{passive:true});window.addEventListener('resize',measure);update();})();(function(){var forms=document.querySelectorAll('.newsletter-form');if(!forms.length)return;var reduce=window.matchMedia&&matchMedia('(prefers-reduced-motion: reduce)').matches;var EMAIL_RE=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;function confettiBurst(origin){if(reduce||typeof document.body.animate!=='function'||!origin)return;var vh=window.innerHeight,N=70;var r=origin.getBoundingClientRect();var ox=r.left+r.width/2,oy=r.top+r.height/2;var endY=vh+50,H=endY-oy;var offs=[0,0.12,0.26,0.42,0.58,0.74,0.88,1];for(var i=0;i<N;i++){var bit=document.createElement('i');bit.className='confetti-bit';var size=6+Math.random()*5;bit.style.width=size.toFixed(1)+'px';bit.style.height=(size*0.42).toFixed(1)+'px';bit.style.left='0px';bit.style.top='0px';bit.style.filter='brightness('+(0.85+Math.random()*0.4).toFixed(2)+')';document.body.appendChild(bit);var ang=Math.random()*Math.PI*2;var speed=90+Math.random()*250;var vx0=Math.cos(ang)*speed;var vy0=Math.sin(ang)*speed;var g=280+Math.random()*130;var T=(-vy0+Math.sqrt(vy0*vy0+2*g*H))/g;var swayAmp=14+Math.random()*22,swayW=(1.5+Math.random()*1.5)*Math.PI,phase=Math.random()*6.28;var rot=Math.round(Math.random()*900-450);var frames=offs.map(function(o){var t=o*T;var x=ox+vx0*t+swayAmp*Math.sin(swayW*t+phase);var y=oy+vy0*t+0.5*g*t*t;return{transform:'translate(calc(-50% + '+x.toFixed(1)+'px), calc(-50% + '+y.toFixed(1)+'px)) rotate('+Math.round(rot*o)+'deg)',opacity:o<0.72?1:Math.max(0,1-(o-0.72)/0.28),offset:o,easing:'linear'};});var delay=Math.random()*120;bit.animate(frames,{duration:Math.round(T*1000),delay:delay,fill:'both'});(function(b,total){setTimeout(function(){if(b.parentNode)b.parentNode.removeChild(b);},total+80);})(bit,Math.round(T*1000)+delay);}}
function retireInlineCards(){Array.prototype.forEach.call(document.querySelectorAll('.newsletter'),function(nl){if(nl.closest('.subscribe-overlay'))return;var f=nl.querySelector('.newsletter-form');if(f&&f.classList.contains('is-done'))return;nl.style.display='none';});}
try{if(localStorage.getItem('newsletter-subscribed')==='1')retireInlineCards();}catch(e){}
document.addEventListener('newsletter:subscribed',function(){setTimeout(retireInlineCards,50);});Array.prototype.forEach.call(forms,function(form){var input=form.querySelector('.newsletter-input');var field=form.querySelector('.newsletter-field');var msg=form.querySelector('.newsletter-msg');var gotcha=form.querySelector('.newsletter-gotcha');var section=form.closest('.newsletter');var endpoint=form.getAttribute('data-endpoint')||'';function setMsg(text,kind){msg.textContent=text||'';msg.classList.remove('is-error','is-success');if(kind)msg.classList.add('is-'+kind);}
function fail(text){setMsg(text,'error');field.classList.remove('shake');void field.offsetWidth;field.classList.add('shake');input.focus();}
function collapse(){section.style.height=section.offsetHeight+'px';section.style.overflow='hidden';void section.offsetHeight;section.style.transition='height 0.5s cubic-bezier(0.22, 1, 0.36, 1), '+'margin-top 0.5s cubic-bezier(0.22, 1, 0.36, 1)';requestAnimationFrame(function(){section.style.height='0px';section.style.marginTop='0px';});}
var inOverlay=!!form.closest('.subscribe-overlay');function lifted(){document.dispatchEvent(new CustomEvent('newsletter:lifted'));}
function dismiss(){var card=section&&section.querySelector('.newsletter-card');if(reduce||!card){lifted();if(!inOverlay&&section)section.style.display='none';return;}
section.style.pointerEvents='none';card.classList.add('is-dismissing');card.addEventListener('animationend',function te(ev){if(ev.animationName!=='ns-liftoff')return;card.removeEventListener('animationend',te);lifted();if(!inOverlay)collapse();});}
function succeed(){form.classList.add('is-done');input.disabled=true;var card=section&&section.querySelector('.newsletter-card');if(card)card.classList.add('is-subscribed');var btn=form.querySelector('.newsletter-submit');if(btn)btn.setAttribute('aria-label','Subscribed');confettiBurst(btn);try{localStorage.setItem('newsletter-subscribed','1');}catch(e){}
document.dispatchEvent(new CustomEvent('newsletter:subscribed'));setTimeout(dismiss,reduce?1500:1800);}
form.addEventListener('submit',function(e){e.preventDefault();if(form.classList.contains('is-done'))return;if(gotcha&&gotcha.value)return;var email=(input.value||'').trim();if(!EMAIL_RE.test(email)){fail('Please enter a valid email address.');return;}
if(endpoint){var body=new FormData();body.append('email',email);try{fetch(endpoint,{method:'POST',body:body,mode:'no-cors'});}catch(err){}}
succeed();});});})();(function(){var btn=document.getElementById('nav-subscribe');if(!btn)return;var overlay=document.getElementById('subscribe-overlay');var closeBtn=document.getElementById('subscribe-close');var reduce=window.matchMedia&&matchMedia('(prefers-reduced-motion: reduce)').matches;try{if(localStorage.getItem('newsletter-subscribed')==='1')btn.classList.add('is-subscribed');}catch(e){}
function showNote(){Toast.show('You’re subscribed!',btn);}
var cardOutline=null,fieldOutline=null;function prepDraw(){if(!overlay||reduce)return;var card=overlay.querySelector('.newsletter-card');var field=overlay.querySelector('.newsletter-field');if(!card||!field)return;if(!cardOutline){cardOutline=Outline.make('draw-card');card.appendChild(cardOutline);}
if(!fieldOutline){fieldOutline=Outline.make('draw-field');field.appendChild(fieldOutline);}
Outline.size(cardOutline,card,20);Outline.size(fieldOutline,field,field.offsetHeight/2);}
var lastFocus=null,closing=false;function openOverlay(){if(!overlay)return;closing=false;lastFocus=document.activeElement;ToggleClose.engage(closeOverlay);overlay.classList.remove('closing');prepDraw();void overlay.offsetWidth;overlay.classList.add('open');overlay.setAttribute('aria-hidden','false');document.documentElement.style.overflow='hidden';var input=overlay.querySelector('.newsletter-input');setTimeout(function(){if(input)input.focus();},reduce?60:1500);}
function closeOverlay(skipOutro){if(!overlay||closing)return;closing=true;document.documentElement.style.overflow='';ToggleClose.startClose();if(lastFocus&&lastFocus.focus)lastFocus.focus();var finish=function(){overlay.classList.remove('open','closing');overlay.setAttribute('aria-hidden','true');ToggleClose.finishClose();closing=false;};if(skipOutro===true||reduce){overlay.classList.remove('open');overlay.setAttribute('aria-hidden','true');setTimeout(finish,800);return;}
overlay.classList.add('closing');setTimeout(function(){overlay.classList.remove('open');},500);setTimeout(finish,950);}
btn.addEventListener('click',function(){if(btn.classList.contains('is-subscribed')){showNote();return;}
openOverlay();});if(overlay){overlay.addEventListener('click',function(e){if(e.target===overlay)closeOverlay();});if(closeBtn)closeBtn.addEventListener('click',closeOverlay);document.addEventListener('keydown',function(e){if(e.key==='Escape'&&overlay.classList.contains('open'))closeOverlay();});}
document.addEventListener('newsletter:lifted',function(){btn.classList.add('is-subscribed','just-subscribed');if(overlay&&overlay.classList.contains('open'))closeOverlay(true);});})();